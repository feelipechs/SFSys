{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development nodemon src/server.js",
    "test": "NODE_ENV=test echo \"Rodando testes...\" && exit 1",
    "start": "NODE_ENV=production node src/server.js",
    "db:migrate": "npm run db:migrate:dev",
    "db:migrate:dev": "NODE_ENV=development npx sequelize-cli db:migrate",
    "db:migrate:undo": "NODE_ENV=development npx sequelize-cli db:migrate:undo",
    "db:migrate:undo:all": "NODE_ENV=development npx sequelize-cli db:migrate:undo:all",
    "seed": "npm run seed:dev",
    "seed:dev": "NODE_ENV=development npx sequelize-cli db:seed:all",
    "seed:undo:dev": "NODE_ENV=development npx sequelize-cli db:seed:undo:all",
    "db:migrate:test": "NODE_ENV=test npx sequelize-cli db:migrate",
    "seed:test": "NODE_ENV=test npx sequelize-cli db:seed:all",
    "seed:undo:test": "NODE_ENV=test npx sequelize-cli db:seed:undo:all",
    "db:migrate:prod": "NODE_ENV=production npx sequelize-cli db:migrate",
    "seed:prod": "NODE_ENV=production npx sequelize-cli db:seed:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "cpf-cnpj-validator": "^1.0.3",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.15.3",
    "prettier": "^3.6.2",
    "sequelize": "^6.37.7",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "sequelize-cli": "^6.6.3"
  }
}
